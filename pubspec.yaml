name: quiz_app_frontend
description: Simple quiz app.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.0.0
  http: ^0.13.5

dev_dependencies:
  flutter_test: 
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
